#include "physics.hpp"
using namespace physics;

cast_hit collider_group::circle_cast(rge::vec2 center, float radius) {
    std::vector<rge::rect*>::iterator it;
    for(it = colliders.begin(); it != colliders.end(); it++) {
        
    }
}